import Client from '../client/client';
import PacketReader from '../packet/PacketReader';

const CharacterSelectWithSPWHandler = (
  client: Client,
  reader: PacketReader
) => {};

export default CharacterSelectWithSPWHandler;
